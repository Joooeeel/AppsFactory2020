import React from 'react';

export default ({ final_result }) => {
    return (
        <div>
            <input type="text" placeholder='resultado....' value={final_result} />
        </div>
    );
};